# @sqlbraid/codegen

Generate TypeScript models from SQLBraid database metadata snapshots.

```sh
npm install @sqlbraid/codegen @sqlbraid/metadata
```

Generate source for application models by passing a validated snapshot and a matching database type policy. This package does not connect to databases.

See the [SQLBraid documentation](https://clickin.github.io/SQLBraid/) for details.
